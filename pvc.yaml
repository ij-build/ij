environment:
  - FOO=BAZ

tasks:
  a:
    image: alpine
    script: |
      echo $FOO

plans:
  x:
    stages:
      - name: foo
        concurrent: true
        tasks:
          - a
          - a
          - name: a
            environment:
              - FOO=BAR
