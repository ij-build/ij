---

definitions:
  stringOrList:
    oneOf:
      - type: string
      - type: array
        items:
          type: string
  fileList:
    type: object
    properties:
      excludes:
        $ref: '#/definitions/stringOrList'
    additionalProperties: false

type: object
properties:
  options:
    type: object
    properties:
      ssh-identities:
        $ref: '#/definitions/stringOrList'
      force-sequential:
        type: boolean
      healthcheck-interval:
        type: string
    additionalProperties: false
  registries:
    type: array
    items:
      type: object
  environment:
    $ref: '#/definitions/stringOrList'
  env_file:
    $ref: '#/definitions/stringOrList'
  import:
    $ref: '#/definitions/fileList'
  export:
    $ref: '#/definitions/fileList'
additionalProperties: false
