---

definitions:
  stringOrList:
    oneOf:
      - type: string
      - type: array
        items:
          type: string
  fileList:
    type: object
    properties:
      excludes:
        $ref: '#/definitions/stringOrList'
    additionalProperties: false

type: object
properties:
  ssh-identities:
    $ref: '#/definitions/stringOrList'
  force-sequential:
    type: boolean
  healthcheck-interval:
    type: string
  registries:
    type: array
    items:
      type: object
  environment:
    type: array
    items:
      type: string
  import:
    $ref: '#/definitions/fileList'
  export:
    $ref: '#/definitions/fileList'
additionalProperties: false
