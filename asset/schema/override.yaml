---

# TODO - other options such as workspace configs, force sequential, etc

title: TODO
description: TODO
type: object
properties:
  registries:
    description: TODO
    type: array
    items:
      description: TODO
      type: object
  ssh-identities:
    description: TODO
    oneOf:
      - type: string
      - type: array
        items:
          type: string
  environment:
    description: TODO
    type: array
    items:
      type: string
  import:
    description: TODO
    type: object
    properties:
      excludes:
        description: TODO
        oneOf:
          - type: string
          - type: array
            items:
              type: string
    additionalProperties: false
  export:
    description: TODO
    type: object
    properties:
      excludes:
        description: TODO
        oneOf:
          - type: string
          - type: array
            items:
              type: string
    additionalProperties: false
additionalProperties: false